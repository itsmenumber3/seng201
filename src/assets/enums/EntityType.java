package assets.enums;

/**
 * The enumerator of the types of entities in this program.
 * An entity is something that can be traded through the shop.
 * An entity can be an item or a monster.
 */
public enum EntityType {
    ITEM,
    MONSTER,
}
