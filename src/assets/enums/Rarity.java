package assets.enums;

/**
 * This enum type provides for three values of Rarity: COMMON, UNCOMMON and RARE.
 * @see entities.Entity the Entity abstract class where this is used.
 */
public enum Rarity {
    COMMON,
    UNCOMMON,
    RARE
}
